// Online C++ compiler to run C++ program online
#include <iostream>
#include <algorithm>
#include <cmath>

using namespace std;

void Rearr(int arr[], int n){
    sort(arr, arr+n);
    
    int temp[n] = {};
    
    for(int i=0;i<n;i++) cout << temp[i] << arr[i] << endl;
    
    for(int i=0;i<n;i++){
        int a = -1;
        int base = (n/2);
        cout << i << " ";
        cout << i*(int)pow(a,i) << endl;
        temp[i] = arr[base-i*(int)pow(a,i)];
        cout << "arr point " << base-i*(int)pow(a,i)<< endl;
        base-=i*(int)pow(a,i);
        cout << "new base " << base << endl;
        cout << endl;
        /*
        if(i%2 != 0){
            cout << "odd"<< endl;
            cout << i << " ";
            cout << i*(int)pow(a,i) << " ";
            temp[i] = arr[base-i*(int)pow(a,i)];
            cout << base-i*(int)pow(a,i)<< endl;
            base = base-i*(int)pow(a,i);
            cout << "new base " << base << endl;
            cout << endl;
        }
        else{
            cout << "even"<< endl;
            cout << i << " ";
            cout << i*(int)pow(a,i) << " ";
            temp[i] = arr[base-i*(int)pow(a,i)];
            cout << base-i*(int)pow(a,i)<< endl;
            base = base-i*(int)pow(a,i);
            cout << "new base " << base << endl;
            cout << endl;
        }
          */  
    }
    cout << endl;
    for(int i=0;i<n;i++) cout << temp[i];
}

int main(){

    int arr[] = {1,3,2,4,5,7,6};
    int n = sizeof(arr)/sizeof(arr[0]);
    
    Rearr(arr, n);

    
}
